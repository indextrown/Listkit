#if canImport(SwiftUI)
/// List-level event storage. Concrete delegate handlers are added in later milestones.
public struct LKListEvents {
    public init() {}
}

/// Section-level event storage. Concrete delegate handlers are added in later milestones.
public struct LKSectionEvents {
    public init() {}
}

/// Row-level event storage. Concrete delegate handlers are added in later milestones.
public struct LKRowEvents {
    public init() {}
}

/// Supplementary-level event storage. Concrete delegate handlers are added in later milestones.
public struct LKSupplementaryEvents {
    public init() {}
}
#endif
